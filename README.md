# bored-cats
making something fun for my cats

```
poetry shell
source .env
poetry install
```

to run the server:

`poetry run uvicorn app.server:app --host 0.0.0.0 --port 8000`

to access the server from the outside i'm gonna use tailscale
